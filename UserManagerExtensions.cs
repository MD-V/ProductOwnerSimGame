using Microsoft.AspNetCore.Identity;
using ProductOwnerSimGame.Models.Users;
using System.Threading.Tasks;

namespace ProductOwnerSimGame
{
    public static class UserManagerExtensions
    {

        /*
        public static async Task<User> FindUserByUserNameOrEmail(this UserManager<User> userManager, string userNameOrEmail)
        {
            return await userManager.FindByNameAsync(userNameOrEmail).ConfigureAwait(false) ??
                   await userManager.FindByEmailAsync(userNameOrEmail).ConfigureAwait(false);
        }
        */

    }
}
